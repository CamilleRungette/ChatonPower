		Hey <%= @current_user.first_name %> ! Thanks for your order n° <%= @order.id %>

		Here is the content of your order : (<%= @order.items.count %> 

		<% if @order.items.count === 1 %>
		<%= @order.items.title %>
		<% elsif @order.items.count > 1 %>
			<% @order.items.each do |item| %>
			<%= item.title %> - <%= item.price %> 
			<% end %>
		<% end %>