<!DOCTYPE html>
<html>
	<head>
		<title></title>
	</head>

	<body>

	<div>
		<div class="banner">		
			<h1 class="text-banner">CHÂTON POWER.</h1>
		</div>
		
		<div class="flex-container">
			<% @item.each do |item| %>
				<% @title = item.title %>
				<% @price = item.price %>
				<% @image = item.image_url %>
				<div>
				<%= render "layouts/card" %></div> <br/> <br/>
			<% end %>
		</div>
	</div>

	</body>
</html>



